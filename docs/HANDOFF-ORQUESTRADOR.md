# HANDOFF — Orquestrador (ondas de execução multi-agente)

Livro de passagem **append-only** da **sessão orquestradora** — a sessão principal do Claude Code que conduz a frota de subagentes (`.claude/agents/`) pelas "ondas". Espelha `docs/HANDOFF.md` (frontend) e `log_agente.md` (backend), mas para a coordenação de **alto nível** que cruza front + back + infra.

> **NUNCA reescreva entradas anteriores. Só acrescente no fim.** O histórico é a memória da orquestração.

---

## 🚦 Você é um orquestrador começando do ZERO? Leia nesta ordem

1. **`CLAUDE.md`** (raiz) — a constituição: o que é o projeto, stack, os 10 princípios invioláveis, convenções, e a seção «Frontend» (identidade v2 "Arkan Claro").
2. **A última entrada do «Log» (fim deste arquivo)** — onde exatamente paramos e qual é a próxima onda.
3. **As «Lições operacionais» abaixo** — como a frota se comporta nesta máquina (custou caro descobrir).
4. **`docs/PLANO_ORQUESTRACAO_ONDAS.md`** — o MÉTODO: grafo de dependências ("o que afeta o que"), mecanismos multi-agente, e o roadmap de ondas.
5. **Planos de produto:** `docs/PLANO_PRODUCTION_READY.md` (12 sprints até o lançamento) · `docs/plano-experiencia-ux-v2.md` (PRs X1–X19 do front).
6. **A frota:** `docs/time_arkan.md` (os 23 agentes, comandos slash, modos de execução, freios).
7. **Memória do Claude Code** (recall automático, se mesma máquina): `project_state` · `orquestracao-frota` · `windows-env-quirks`.

**Seu papel:** você é o **engineering manager**. Quebra a próxima onda em frentes (pelo grafo do §1 do plano), despacha subagentes da frota, valida o resultado, e **PARA nos freios**. Os subagentes coordenam **pelo repositório** (estes arquivos), nunca por chat — o único canal pai→subagente é o prompt do despacho.

**Freios (NUNCA sem o PO Lorenzo):** `git push`, merge remoto, transmissão fiscal real (NF-e/eSocial/SPED), alteração de alíquota seedada, deploy, cobrança.

---

## ⚙️ Lições operacionais (LER SEMPRE — variam por sessão)

1. **Quem roda shell VARIA por sessão — teste no início** (`git status` / `echo hello`). Em 2026-06-17: o ORQUESTRADOR tinha Bash (git bash) funcional; os SUBAGENTES NÃO tinham shell (só Read/Grep/Glob). Mandar subagente "rodar pytest/git e commitar" → ele **TRAVA** (um `reviewer` gastou 39 min / 323 tool calls). **Padrão: subagente escreve/analisa → orquestrador roda/valida/commita.**
2. **PowerShell via Bash é bloqueado** (deny rule). Mas `poetry`/`pytest`/`git`/`npm`/`docker` **direto no git bash funcionam** — para o poetry, antes: `export PATH="/c/Users/loren/AppData/Roaming/Python/Scripts:$PATH"` (Device Guard).
3. **Worktrees do harness (`isolation:worktree`) partem de uma base ANTIGA** (não o HEAD). Ao mergear, conflito só em logs append-only → `sed -e '/^<<<<<<< /d' -e '/^=======$/d' -e '/^>>>>>>> /d'`. Diff real de uma branch: `git diff $(git merge-base hardening <branch>)..<branch>`.
4. **`.claude/agents/*` é gitignored** (config local; recarrega só ao reiniciar a sessão) → para mudar comportamento de agente já carregado, passe o contexto novo no **PROMPT do despacho** (o `CLAUDE.md`, lido em runtime, propaga o resto).
5. **Subagentes não commitam** (sem shell) → o orquestrador **preserva o trabalho deles** (commita no worktree do agente) e **valida no tree principal** (que tem `.venv`/`node_modules`). Padrão p/ validar sem trocar de branch: `git checkout <branch> -- <paths>` → build/pytest → reverter.
6. **Merge com tree sujo** (o PO costuma ter trabalho não-commitado): integrar num **worktree dedicado**, ou `stash -u` → `branch -f hardening <onda>` → `checkout` → `stash pop`.

---

## 📍 Estado atual (resumo — detalhe na última entrada do log)
- **Branch viva:** `main` @ `2286e4a` (= `hardening-fiscal-2026-06`; tudo commitado, frota + infra versionadas; **NÃO pushada**).
- **Backend:** roadmap 0–22 completo; em hardening + production-ready. Integração 24/24 (fixtures RLS corrigidos).
- **Frontend:** identidade v2 "Arkan Claro" — **D0→D4 COMPLETO** + Onda 4 front: **X17** monitores de limite · **X19** motion dos overlays (animam, @utility) · **X16** assistente (já real; label) · **X15** onboarding CNPJ-first (dashboard imediato) + fix do 422 que quebrava o onboarding. App ligado ao backend real :8000.
- **Onda 4 restante (depende do PO):** X14 "fechar o mês" (backend S5) · X18 brand pack · LLM no dev p/ o assistente responder (Ollama/Gemini) · "pedir o resto em contexto" do onboarding (refinamento).
- **Backlog:** segurança do webhook · teste de integração do webhook · largura da coluna Contraparte na DataTable · timeout/erro no fetch do assistente (hoje fica "digitando" sem LLM).

---

## Log (append-only)

### 2026-06-17 · orquestrador · Ondas 1 + 1.5 + consolidação + limpeza
- **Etapa 0:** recalibrou a frota de design (`CLAUDE.md §Frontend` + agentes) da v1 "Instrumento" → v2 "Arkan Claro" (número-herói mono, crop marks raros, respiro, gates §5).
- **Onda 1 (3 frentes paralelas):** **F1** = traduções fiscais (`analista-fiscal-web/src/lib/traducao/*`) + urgência 3 níveis; **F2** = tokens v2 (`globals.css` + `layout.tsx`, dark re-derivado, AA nos 2 temas); **F3** = webhook Pluggy → fila Celery (idempotente, tenant-aware). Cada frente: 1 subagente em worktree/background; o orquestrador validou (build verde, pytest 60 + mypy) e commitou (os subagentes não tinham shell).
- **Onda 1.5 (validação profunda):** 5 gates frescos (`reviewer`×2, `backend-reviewer`, `security-auditor`, `content-fiscal`) + integração (`qa-integration` por análise + pytest rodado pelo orquestrador). Achados tratados: **4 correções fiscais** aplicadas (DCTFWeb inclui patronal; REINF precisa retenções; Fator R "desconto"→"alíquota menor"; Anexo IV exemplos corretos). Tooling QA novo no CI: Lighthouse/axe + Playwright visual + Semgrep/Gitleaks. **Parecer: VERDE.**
- **Consolidação:** as 4 frentes integradas em `feat/onda-1` (worktree dedicado), promovidas para `hardening-fiscal-2026-06` via fast-forward. **Raiz e `docs/` limpos** — superados/consumidos arquivados em `legacy/` (ver `legacy/README.md`). Worktrees + branches de trabalho removidos (housekeeping). Git enxuto: só `hardening-fiscal-2026-06` + `main`.
- **Estado:** `hardening-fiscal-2026-06` @ `549804d` tem TUDO. **NÃO pushado** (ato do PO). Pendências do PO preservadas no working tree, **não-commitadas**: IBGE (`empresa/cnpj.py`, `schemas.py`, `tabelas_admin/*`, `onboarding/passo-cnpj.tsx`), migration `0058` (INSS 2026) + `test_calcula_inss_2026`, frota (`.github/ci.yml`, `.mcp.json`, `.pre-commit-config.yaml`, `.claude/commands/*`), business (`docs/negocio/`, `docs/time_arkan.md`).
- **Backlog aberto:** (1) **segurança do webhook** — rate limit no webhook público, cap de body, AES-256 de coluna (PII bancária), ADR do drain cross-tenant antes de ligar Celery real; (2) **teste de integração do webhook F3** (só há unit); (3) **fixtures RLS** (`test_rls_isolation.py`) quebrados porque criam empresa sem IBGE (agora obrigatório) — do workstream IBGE, urgente p/ a CI de integração.
- **Próximo orquestrador → faça:** **Onda 2** = `X7 primitivas v2 → X8 gabarito Notas` (SERIAL, tree principal — worktree de tela só enxerga o design-system se ele estiver na base). `design-system` (X7) → `screen-implementer` (X8, Notas = gabarito de ouro), `reviewer` (gates v2) entre cada. Só após X8 mergeado os lotes de tela explodem em paralelo (Onda 3). NFS-e Nacional no backend quando o PO credenciar o ADN. Roadmap: `docs/PLANO_ORQUESTRACAO_ONDAS.md §4` + `docs/plano-experiencia-ux-v2.md`.

### 2026-06-17 · orquestrador · fechamento: versiona infra + WIP, consolida na `main`
- **Versionou a infra de orquestração** (fecha a lacuna de portabilidade detectada na auditoria): a frota `.claude/agents` (24 agentes) saiu do `.gitignore`; entraram `.mcp.json`, `.github/workflows/ci.yml`, `.pre-commit-config.yaml`, os comandos slash, `docs/time_arkan.md`, `docs/negocio`. Varredura de segredos: só credenciais dev/localhost/CI (`postgres:postgres`, `fiscal:fiscal@localhost`) — nada de produção.
- **Commitou o WIP de feature** antes solto no working tree: IBGE obrigatório (`empresa/cnpj.py`, `schemas`, `passo-cnpj`), `tabelas_admin/salario_minimo` + testes, migration `0058` (INSS 2026) + `test_calcula_inss_2026`. ⚠️ **~5 testes de `tests/integration/test_rls_isolation.py` ficam VERMELHOS** até os fixtures passarem o código IBGE — é o primeiro item a corrigir.
- **Consolidou tudo na `main`** via fast-forward: `main` = `hardening-fiscal-2026-06` = `28df342`. Working tree limpo (nada solto). **NÃO pushado** (ato do PO).
- **Resultado:** o próximo orquestrador agora retoma de **qualquer máquina** (frota + infra + handoffs todos versionados). A próxima onda continua a mesma: **Onda 2 (X7 → X8)**.
- **Próximo orquestrador → faça:** (0) corrigir os fixtures RLS quebrados pelo IBGE; depois a Onda 2.

### 2026-06-18 · orquestrador · item 0 (fixtures RLS) + Onda 2 (X7 → X8)
- **Ambiente desta sessão:** o ORQUESTRADOR tem Bash (git/poetry/npm/docker) funcional; os SUBAGENTES seguem **sem shell** (write-only). Padrão mantido: subagente escreve → orquestrador valida/commita. Docker de pé (postgres+redis 25h).
- **Item 0 — fixtures RLS (`7b71777`):** `tests/integration/test_rls_isolation.py` criava empresa sem `codigo_municipio_ibge` (NOT NULL desde a migration 0049) → 422 antes do 201/409. Corrigido: fixtures e POST duplicado enviam "3550308"; o INSERT cru de `test_with_check...` fornece o IBGE de propósito (única razão de falha = RLS WITH CHECK, não NOT NULL). **Integração: 24 passed** (era o único arquivo quebrado pelo IBGE).
- **Gate de build do front (`4c442d7`):** descoberto que `next build` falhava para QUALQUER onda — o `tsconfig` incluía `tests/{a11y,visual}` + `playwright.config.ts` (tooling de QA da Onda 1.5, deps só na CI) no typecheck. Excluídos do build da app. **`next build` agora VERDE.**
- **X7 — primitivas v2 (`d5a08fe`), `design-system`:** recalibração (não rebuild) das primitivas: botão 44px verde + press spring, `DataTable` (tabela→card mobile, NOVA), `RulerGauge` (assinatura nº2, irmão do Ruler intocado), `useCountUp`, `BottomTabBar` mobile fiada no layout. API preservada. Gate `reviewer`: **VERDE**. Ressalva → backlog motion-polish/D6: spring de entrada de dialog/sheet inerte por dívida pré-existente (`tailwindcss-animate` nunca registrado no Tailwind v4) — NÃO regressão.
- **X8 — gabarito Notas (`0324de1`), `screen-implementer` + correções do orquestrador:** número-herói (faturamento do mês, useCountUp), 1 ação primária 44px, painel SEM crop marks (label Hanken no lugar de "Fig."), DataTable, Carimbo pós-ação já existia. Gate `reviewer`: **VERMELHO** — pegou um blocker real: o stretched-link da `DataTable` capturava o clique do dropdown DANFE/XML (desktop+mobile; no mobile o card era `<a>` com `<button>` aninhado). **Orquestrador corrigiu** a primitiva (flag `interactive`+`relative z-10`; mobile link vira irmão `absolute inset-0`; `getRowLabel` p/ a11y) + limpou nav redundante + baixou o piso do `clamp` do herói (estourava em 360–390px). **Verificação visual (Playwright @ :3000): blocker RESOLVIDO em ambos os layouts**, tab bar mobile + tabela→card + herói OK. Parecer pós-correção: **VERDE**.
- **Lição operacional nova:** o **code-review (estático) não pega bugs de render** — o stretched-link×dropdown e o overflow do herói só apareceram na **verificação visual (Playwright)**. Para o gabarito (e telas que serão copiadas), rodar o browser vale o custo. Auth do front bate no **backend real :8000** (via `.env.local`, não mock) e o CORS só aceita **:3000**; um dev server em :3001 (porta fallback) é bloqueado por CORS no login. Use a instância :3000 para verificação visual (login demo: `demo`/`demo@arkan.dev`/`arkan1234`).
- **Estado:** `main` @ `0324de1`, working tree limpo, **NÃO pushado** (ato do PO). Backlog do webhook (segurança + teste de integração) segue aberto, não tocado nesta onda.
- **Próximo orquestrador → faça:** **Onda 3** = lotes de tela D4 (A/C/D/E) em PARALELO — a base já tem o design-system (X7) + o gabarito (X8), então worktrees de tela enxergam tudo. 1 worktree/lote, `screen-implementer` por lote imitando o gabarito Notas, `reviewer` (gates v2 + **verificação visual Playwright @ :3000**) por PR. Home v2 (número-herói + RulerGauge no monitor do teto do Simples). Ressalva de largura da coluna Contraparte (DataTable) a tunar quando os lotes adotarem. NFS-e Nacional no backend quando o PO credenciar o ADN.

### 2026-06-20 · orquestrador · Onda 3 (D4) COMPLETA — home + lotes A/C/D/E
- **Decisão de execução (o PO escolheu):** os subagentes seguem **write-only** e a validação é **serial** (1 dev server, login só em :3000 por CORS, não dá `next build` com dev vivo, worktree branca de base antiga sem design-system). Logo, **NÃO** foi paralelo: **1 Opus focado POR lote, em SÉRIE**, cada um validado (tsc + verificação visual Playwright + `reviewer` de contexto fresco) e commitado antes do próximo. Rendeu mais em qualidade que a corrida de escrever em paralelo (que aqui quase não acelera).
- **6 commits** (todos na `main`, **NÃO pushados**): `4c442d7→…` não; nesta onda: gate de build (já na Onda 2), **X9 home** (`586ae8a`), **X10 lote A Fiscal** (`c0e2d59`), **X11 lote C Controles+Contábil** (`8513936`), **X12 lote D Pessoal+Relatórios** (`102ed22`), **X13 lote E Compliance/Agenda/Config/Assistente/Onboarding** (`7b50b96`).
- **Padrão v2 aplicado e consolidado** em ~40 telas: número-herói mono (onde há valor dominante; nunca forçado em ferramenta/listagem/config), 1 ação primária verde 44px, `<Fig>`→label de seção Hanken, `Framed marks={false}` em painel comum (crop marks MANTIDOS só em detalhe/print/confirmação = assinatura rara legítima §3), DataTable com `interactive:true`+`getRowLabel` onde há ação por linha, Carimbo em estado resolvido, `ink-3` nunca em dado (→ink-2), siglas sempre traduzidas (CFOP/CST/NCM/S-XXXX/CND/CRF nunca cruas), charts re-tematizados mesmos dados.
- **Gates por lote: TODOS VERDES** (X9 e X10 e X11 VERDE; X12 e X13 AMARELO→VERDE após corrigir `ink-3`-em-dado e o glow hardcoded). O `reviewer` pegou o que o code estático esconde mas a verificação visual confirmou; principais achados que o orquestrador corrigiu na hora: stretched-link×dropdown na DataTable (X8, primitiva), overflow do herói no mobile (X9), 5 `<Fig>` residuais na home (X9), vários `ink-3`-em-dado, glow/verde-hardcoded no wizard-shell (X13).
- **Invariantes preservados:** conciliação (match) intacta, wizard-admissão (RHF+Zod) e holerite PDF intactos, onboarding (store/validação) e assistente (mock/citação) intactos, PIX/DANFE/XML funcionando, guard de onboarding ativo.
- **Gate final de produção:** `npm run build` **VERDE** — Compiled successfully + **Generating static pages (48/48)** + exit 0 (rodado com o dev server morto; ver lição abaixo).
- **Lição operacional (cara):** rodar `npm run build` (produção) com o **dev server vivo no mesmo `.next`** o corrompe (404 nos chunks / "Cannot find module"). Aconteceu 2×. Fix: matar o dev → (se preciso) `node -e "fs.rmSync('.next',{recursive:true,force:true})"` (o `rm -rf` é bloqueado pelo sandbox) → reiniciar dev. **Regra: gate por lote = tsc + render no dev; `next build` só em ponto controlado com o dev morto.**
- **Estado:** `main` @ `7b50b96` (+ este write-back), working tree limpo, **NÃO pushado** (ato do PO). `.next` é de produção (build final); um `npm run dev` recompila por cima.
- **Próximo orquestrador → faça:** Onda 4 = o que sobra do roadmap, em grande parte dependente de credenciais/backends do PO: **X14** "fechar o mês" guiado (depende da orquestração backend S5 eSocial→Reinf→DCTFWeb), **X15** onboarding CNPJ-first (lógica/BrasilAPI — a pele já é v2), **X16** assistente real (religar backend — a pele já é v2), **X17** monitores de limite (RulerGauge já existe), **X18** brand pack (logo/landing/brand book), **X19** motion polish (D6: registrar `tailwindcss-animate` OU Framer nos overlays — a entrada de dialog/sheet segue inerte; dark validado por humano; Lighthouse). Backlog técnico aberto: segurança do webhook + teste de integração do webhook (Onda 1.5); largura da coluna Contraparte na DataTable.

### 2026-06-21 · orquestrador · Onda 4 (parcial) — X17 ✅ · X19 investigado→revertido
- **Decisão do PO:** "1 Opus focado por lote, em série" (mesmo método da Onda 3); subagentes write-only, orquestrador valida (tsc + Playwright @ :3000) e commita.
- **X17 — monitores de limite (`/fiscal/limites`) ✅ commitado `acc42e4`:** tela nova reusando RulerGauge + `useApuracaoAtual` (nada inventado): número-herói (faturamento 12m), réguas MEI/sublimite/teto com projeção, medidor de Fator R (só Anexo III/V), aviso de desenquadramento traduzido, Carimbo. Subnav fiscal ganhou "05 Limites" + `overflow-x-auto` (mobile). Fluxo: `explorer` mapeou → `screen-implementer` construiu → `reviewer` VERDE → verificação visual desktop+mobile. Nuance: a empresa em runtime (backend :8000) é **Comércio Anexo I** (o `empresa-demo.ts` estático diz Anexo III, mas não é a fonte) → Fator R/MEI corretamente ocultos p/ esse perfil.
- **X19 — motion dos overlays: ATTEMPT→REVERT.** `motion-polish` escreveu Plano B (keyframes CSS + utilidades em `@layer utilities`). **Verificação no browser provou que NÃO anima** (`getComputedStyle(dialog).animationName === "none"`): no **Tailwind v4**, utilidade em `@layer utilities` **não gera** a variante `data-[state=open]:animate-in` que os componentes usam — só `@utility` gera. Revertido (não shipar CSS morto). Diagnóstico + plano de retomada (Opção A: converter p/ `@utility`; Opção C **recomendada**: Framer `AnimatePresence`+`forceMount` spring 260/32) em `docs/HANDOFF.md`. O overlay ink/35+blur do §4 **já funcionava** desde o X7. **X19 é PR dedicado, não polish-rápido.**
- **Lição nova:** **validar motion no browser com `getComputedStyle`/`evaluate`**, não confiar no relatório do subagente (ele não tem shell p/ testar) — o Plano B "parecia certo" mas era inerte. E: **Tailwind v4 = `@utility` (não `@layer utilities`) para utilidade que precisa de variante** (`data-[state]:`, `hover:`, etc.).
- **Estado:** `main` @ `acc42e4` + write-back; working tree limpo; **NÃO pushado**. Dev server vivo em :3000.
- **Próximo:** Onda 4 restante conforme o PO: X19 via PR dedicado (Framer) · X15/X16 (lógica/backend) · X18 brand pack. Backlog técnico segue.

### 2026-06-21 · orquestrador · Onda 4 front COMPLETA (X19 re-feito, X16, X15) — ordem a→b→c do PO
- O PO pediu "seguir a ordem a, b, c". **(a) X19** re-feito e ENTROU (`8d2dc1c`): Opção A (`@utility` no `globals.css` — a forma que o Tailwind v4 exige p/ gerar a variante `data-[state=open]:`). Insight: v4 usa a propriedade CSS `translate` (≠ `transform`) p/ `-translate-1/2`; keyframe anima só `transform`, sem base-offset. Verificado no DOM (dialog anima + centerX=640). **(b)** backends confirmados (:8000, 177 rotas; app já-real) + **X16** (`293bd56`): assistente JÁ era real, removido o label "Mock" falso (LLM no dev é ops). **X15** (`2286e4a`): onboarding CNPJ-first com dashboard imediato ("Ir para o painel" após o CNPJ) + **corrigido um 422 pré-existente que quebrava TODO o onboarding** (`faturamento12m` extra_forbidden no body). Verificado ponta-a-ponta com conta nova + BrasilAPI viva → /home da empresa nova.
- **Lições novas:** (1) **Tailwind v4: `@utility` (não `@layer utilities`)** p/ utilidade com variante. (2) v4 `-translate-*` = propriedade `translate`. (3) validar **motion no browser** (`getComputedStyle`) — relato de subagente sem shell não basta. (4) ler o **`response-body` do 422** acha contrato quebrado na hora. (5) muitos restarts de dev server thrasham o `.next`/browser — `node fs.rmSync('.next')` + reload limpo.
- **Estado:** `main` @ `2286e4a`, working tree limpo, **NÃO pushado**. Dev server :3000 vivo.
- **Próximo orquestrador → faça:** o que resta da Onda 4 depende do PO (X14 backend S5, X18 brand pack, LLM no dev). (c) é ato do PO: revisar/pushar.

### 2026-06-22 · orquestrador · Auditoria fiscal + Produção M1/M2 — `main` PUSHADA
Sessão longa, **toda backend**, conduzida pelo orquestrador SOLO (os subagentes bateram **529 Overloaded** de forma recorrente — ver lição). **O PO liberou `git push`**: `main` agora está no GitHub (`lorenzo265/FiscalAI`), sincronizada @ `153ac30`.

- **Auditoria fiscal implacável** (skill `auditor-fiscal-implacavel`): 7 auditores A1–A7 → `docs/auditoria-fiscal/auto-de-infracao-2026-06-21.md` (7🔴 8🟠 7🟡 33🔵). Depois CORRIGIDA em ondas:
  - **Onda A** (`ab62b8d`): Fator R com encargos · PGDAS folhasSalario · DRE sem dupla contagem 5.2/5.3 · lançador valida partida dobrada · **dividendos retenção 10%** (Lei 15.270/2025).
  - **Onda B** (`a0c81b1`+`4e88beb`+`095cbd4`): **IRRF 2026** — migration `0059` (vigência SCD, isento R$2.428,80, Lei 15.191/2025) + **redutor Lei 15.270/2025** ligado em TODA a folha por competência ≥2026-01; método batido com os Exemplos 4/5 oficiais da RFB; fix de interação (folha IRRF=0 não emite partida zerada — pego só pela integração).
  - **Onda C** (`d917f0d`+`bfe1351`): Simples/MEI fora da Reforma 2026 · RBT12 proporcionalizado no início de atividade · parcelamento posterga p/ dia útil · imobilizado coerência taxa×vida · reconciliação NF (Σitens vs **vProd**, não vNF) + CFOP/NCM cabeçalho + CST×CSOSN como log não-bloqueante.
  - **Follow-up** (`b6c72da`): persiste a retenção de dividendos (migration `0060` + coluna em `distribuicao_lucros` + `soma_retencao_no_mes`).
- **Auditoria de production-readiness** (workflow, 8 dimensões) → `docs/PRODUCTION_READINESS_AUDIT-2026-06-21.md`. Plano de execução: `docs/PLANO_GO_LIVE.md` (ações PO §A × orquestrador §B).
- **M1 — Fundação** (`330849b`): Sentry (`send_default_pii=False`) + Prometheus `/metrics` + `CorrelationIdMiddleware` (X-Request-ID nos contextvars do structlog) · **Celery worker/beat REAIS** (`infra/docker/Dockerfile.{worker,beat}` + `docker-compose.prod.yml`; build validado: Celery real, 21 tasks) · CI hardening (type-check do front + cobertura).
- **M2 — Billing/assinatura Stripe** (`153ac30`): módulo `app/modules/billing/` (planos mock 149/299/499 + trial 14d; migration `0061` com RLS + GRANT explícito `fiscal_app`; `StripeProvider` valida `Stripe-Signature` + `_FakeBillingProvider`; service idempotente + webhook `/v1/webhooks/stripe`). Grupo opcional `[billing]` (stripe ^11). **Integração billing ponta-a-ponta 2/2.**
- **Estado:** `main` @ `153ac30` **PUSHADA**. **Suite 2700 passed, 3 skipped · integração +2 billing · mypy strict 0 (365 arq) · ruff ✅.** Migrations 0059/0060/0061 aplicadas no DEV (`alembic upgrade head`). Branches de trabalho removidas.
- **Lições operacionais (caras):** (1) **Subagentes instáveis (529) → faça SOLO**; quando rodarem, ainda erram cálculo de cabeça (golden 672 vs 673 correto) e fixtures XML malformadas — pytest verde só prova consistência interna, **valide à mão + integração**. (2) **GRANT a `fiscal_app` deve ser EXPLÍCITO na migration** — o `ALTER DEFAULT PRIVILEGES` do `init.sql` NÃO cobre tabelas criadas via alembic (a 0061 e até a `cobranca_consulta` ficaram sem; só `distribuicao_lucros` herdou). (3) **RLS de tabela com webhook externo = ENABLE (não FORCE)** — o webhook usa `get_webhook_session` (superuser, bypassa RLS). (4) Mudança de alíquota/tabela seedada = FREIO → fluxo gated (mas o PO já aprovou IRRF nesta sessão).
- **Próximo agente → M3 (LGPD/segurança):** ver o prompt completo em `docs/PROMPT-PROXIMO-AGENTE-M3.md`. Depois M4 (S3 storage, Reinf→SERPRO, e-mail). Tudo código do orquestrador; o PO faz as credenciais/infra (deploy AWS, Stripe Prices, cert ICP-Brasil, etc. — `docs/PLANO_GO_LIVE.md §A`).

### 2026-06-22 · orquestrador · Marco 3 (LGPD & Segurança) — início; PR 6.1 security headers
Sessão backend, orquestrador SOLO (subagentes seguem instáveis/529 → política mantida: faço e valido tudo eu mesmo). Branch nova: `feat/m3-lgpd-seguranca` (a partir de `main @ 0a076f2`). **NÃO pushada.**
- **Confirmação de estado (baseline M3):** pytest **2700 passed, 3 skipped**; mypy strict 0 (365 arq); alembic head `0061` (DB DEV em 0061); Docker up (api+postgres+redis healthy). **Achado novo (ruff):** `ruff check` (= gate bloqueante do `ci.yml`) acusa **~1607 findings pré-existentes** — 1101 são RUF001/002/003 (ruído de tipografia portuguesa: em-dash `—`, `×`, aspas curvas em comentários/docstrings; NÃO auto-corrigíveis; o `→` NÃO é flagged). Contradiz o "ruff ✅" dos handoffs anteriores: o gate de lint do `main` provavelmente está vermelho ou é tolerado. **Não é escopo do M3.**
- **Delegação (decisão do PO):** a faxina de ruff foi delegada a uma **sessão separada** (`spawn_task` → `chore/ruff-lint-cleanup`, worktree próprio) com prompt autocontido: ignore RUF001/2/3 no pyproject + `extend-immutable-calls` p/ o B008 do FastAPI Depends + `ruff check --fix` nos mecânicos, sem mudar lógica, mantendo pytest/mypy verdes, rebase por cima do M3. Mais confiável que um subagente Agent (instável). **Política do M3:** meus arquivos novos/tocados saem `ruff check`-limpos.
- **PR 6.1 — Security headers middleware (✅, validado):** `app/shared/middleware/security_headers.py` (espelha `correlation_id.py`; HSTS injetado por construtor, middleware agnóstico de config). Headers via `setdefault`: nosniff, X-Frame DENY, Referrer-Policy, X-XSS-Protection 0, Permissions-Policy, CSP `default-src 'none'` (isento em `/docs`,`/redoc`,`/openapi.json`), HSTS 2 anos só em staging/prod. Plugado no `main.py` entre CORS e Correlation (aninhamento `Correlation → Security → CORS → RateLimit → app`; headers caem nas 429/erros). Golden de 4 casos. **pytest 2704 passed, 3 skipped (+4); mypy 0 (366 arq); ruff limpo nos meus arquivos; integração 26 passed.** Sem migration (não toca DB).
- **Estado:** `feat/m3-lgpd-seguranca` com o commit do PR 6.1; working tree limpo; **NÃO pushado** (freio — aguarda OK do PO). Próximo M3: **6.2 endpoints LGPD** (`/v1/lgpd/exportar` + `/v1/lgpd/excluir` com anonimização respeitando imutabilidade fiscal §8.2 + retenção 5 anos; tabela de auditoria `lgpd_solicitacao`, migration **0062**), depois 6.3 AES-256 PII, 6.4 refresh token JWT, 6.5 CI gates.

### 2026-06-22 · orquestrador · M3 — colisão de working tree resolvida + PR 6.2a (export LGPD)
**CORREÇÃO ao registro anterior (lição cara):** o `spawn_task` da faxina de ruff **NÃO isolou em worktree próprio** como eu supus — rodou no **working tree principal compartilhado**: fez `checkout -b chore/ruff-lint-cleanup` + `ruff --fix` em ~170 arquivos, trocando a branch debaixo dos meus pés e embolando meus 5 arquivos do PR 6.1 com os 170 da faxina. **Resolução sem destruir o trabalho da faxina (que o PO confirmou estar VIVA no tree principal):** capturei só meus 5 arquivos via `git diff --cached` (os 170 não-staged ficam de fora), criei uma **worktree isolada** `C:/dev/m3-pr61-wt` na minha branch, apliquei o patch lá e commitei (`40cba4c`). Toda a sequência M3 daqui em diante roda nessa worktree isolada (com `.venv` próprio via `poetry install --with dev` + `.env` copiado do tree principal — gitignored, não vem no `git worktree add`; DB é o mesmo Postgres :5434).
- **Lições novas:** (1) `spawn_task` pode rodar no **mesmo checkout** (não garante worktree isolada) → para trabalho concorrente, o orquestrador deve criar a worktree isolada ELE MESMO antes. (2) `git worktree add` **não traz** arquivos gitignored (`.env`, `.venv`) → copiar `.env` do tree principal + `poetry install` na worktree. (3) `git diff --cached` isola cirurgicamente os arquivos staged mesmo num tree contaminado por mudanças não-staged de outro agente.
- **Contaminação pendente (ação do PO/faxina):** meus 5 arquivos do PR 6.1 ainda estão fisicamente no tree principal (branch chore), misturados. **A faxina NÃO deve commitá-los** — antes do commit dela, descartar: `git checkout HEAD -- analista-fiscal-api/app/main.py log_agente.md docs/HANDOFF-ORQUESTRADOR.md` + `rm analista-fiscal-api/app/shared/middleware/security_headers.py analista-fiscal-api/tests/unit/middleware/test_security_headers.py`.
- **PR 6.2a — Export LGPD/portabilidade (✅, validado, commit nesta worktree):** módulo `app/modules/lgpd/` + `GET /v1/lgpd/exportar` (`SessionDep` RLS-isolado) → JSON de TODOS os dados do tenant (12 entidades + tenant) + grava `lgpd_solicitacao`. **Migration 0062** (`lgpd_solicitacao`: RLS + GRANT). Serializador genérico via `inspect()` do mapper (todas as colunas menos `senha_hash` e blobs binários). Todas as 12 entidades têm `tenant_id` (verificado → RLS isola). **🐞 Bug latente CORRIGIDO na 0062:** `guia_pagamento` (Sprint 2) tinha RLS mas **ACL vazia (zero GRANT a `fiscal_app`)** — o módulo `lucro_presumido` a lê/escreve sob `get_session`, então o **fluxo de DARF do LP estava quebrado em prod** e nenhum teste de integração cobria; a 0062 concede o CRUD. Golden `tests/unit/lgpd/test_serializar.py` (7) + integração `tests/integration/test_lgpd.py` (2: estrutura/audit + isolamento RLS cross-tenant; o INSERT em `lgpd_solicitacao` sob `fiscal_app` valida o GRANT+RLS WITH CHECK). **pytest 2711 (+7) · integração 28 (+2) · mypy 0 (371 arq) · ruff limpo nos meus arquivos.**
- **Estado:** worktree isolada `C:/dev/m3-pr61-wt` na `feat/m3-lgpd-seguranca` (commits `40cba4c` PR6.1 + 6.2a a commitar agora); DB :5434 em `0062`. **NÃO pushado** (freio). Próximo: **6.2b** (`POST /v1/lgpd/excluir` por anonimização, respeitando §8.2 + retenção 5 anos), depois 6.3/6.4/6.5.
- **⚠️ Para o PO:** (a) o gate `ruff check` do CI está vermelho no `main` por ~1607 findings pré-existentes (faxina em curso resolve); (b) `guia_pagamento` estava com ACL vazia — bug latente do LP corrigido na 0062.

### 2026-06-22 · orquestrador · M3 — PR 6.2b (esquecimento por anonimização)
Mesma worktree isolada `C:/dev/m3-pr61-wt`. **Nota infra:** os containers Docker (`fiscal_*`) foram parados a meio caminho (api saiu 137, postgres/redis exit 0 — provável `compose down` externo); subi `fiscal_postgres`+`fiscal_redis` de novo (`docker start`), Postgres :5434 healthy. O DB de dev é compartilhado entre as worktrees.
- **PR 6.2b — `POST /v1/lgpd/excluir` (✅, validado):** direito ao esquecimento por **anonimização** (LGPD art. 18, VI) **sem violar a imutabilidade fiscal §8.2 nem a guarda de 5 anos** — as linhas PERMANECEM (valores fiscais intactos), só a PII de pessoas naturais é sobrescrita. Módulo puro `app/modules/lgpd/anonimizacao.py` (token hex determinístico/único derivado do `id` da linha → irreversível + respeita as UNIQUE constraints; `email_anonimo` `.invalid` RFC 6761; `cpf_anonimo` 11 dígitos). Service anonimiza usuario (email/nome + **invalida senha_hash** via `hash_senha(token aleatório)` → login impossível + `ativo=False`), socio/funcionario (nome/cpf), `empresa.whatsapp_phone`→NULL, desativa o tenant, grava `lgpd_solicitacao` (tipo exclusao, status `agendada`, `expurgo_apos`=hoje+5a). Confirmação obrigatória `confirmar: Literal[True]` (sem ela → 422). **Verificado proativamente** que `fiscal_app` tem UPDATE nas 5 tabelas de PII (sem surpresa de grant). Golden (5) + integração (1: 422→anonimiza→linha permanece no export com PII apagada→login original bloqueado).
  - **Nuance validada à mão:** o login pós-erasure retorna **404** (não 401) — `TenantRepo.por_slug` filtra `ativo.is_(True)`, e a exclusão desativa o tenant → conta some do login. É o erasure correto; o teste aceita 401|404.
- **Validação:** pytest **2716 passed, 3 skipped (+5)** · integração **29 passed (+1)** · mypy 0 (372 arq) · ruff limpo nos meus arquivos.
- **Estado:** `feat/m3-lgpd-seguranca` (worktree isolada) = `0a076f2` + 6.1 + 6.2a + 6.2b (a commitar). DB :5434 em `0062`. **NÃO pushado** (freio). Contaminação do PR 6.1 no tree principal/chore segue pendente (comandos no registro anterior). Próximo M3: **6.3 AES-256 em repouso (PII)** — `app/shared/crypto/` envelope, 1 coluna de prova (ex. CPF de socio) via TypeDecorator/migration 2-fases, sem quebrar goldens; depois 6.4 refresh JWT, 6.5 CI gates.

### 2026-06-22 · orquestrador · M3 — PR 6.3 (AES-256 em repouso)
Mesma worktree isolada. **Decisão de design com o PO (importante):** o briefing sugeria cifrar CPF, mas validando à mão CPF tem **UNIQUE (`uq_socio_empresa_cpf`) + lookup por valor** — AES-256-GCM é não-determinístico (nonce por escrita) → quebraria a constraint e o lookup. O PO escolheu a opção recomendada: **coluna de prova = `empresa.whatsapp_phone`** (PII real, SEM constraint/lookup) → risco zero aos goldens. CPF cifrado fica p/ PR dedicada futura (com blind index).
- **PR 6.3 (✅, validado):** `app/shared/crypto/envelope.py` (AES-256-GCM: nonce 12B aleatório, token `v1:base64(nonce|ct|tag)`, autenticado) + `PiiCifrada` TypeDecorator (`pii_type.py`, cifra/decifra transparente, impl Text). Chave `settings.PII_ENCRYPTION_KEY` (base64 32B; default DEV placeholder + **fail-fast em prod** espelhando o JWT_SECRET; KMS em prod). Model `empresa.whatsapp_phone` → `PiiCifrada()`. Migration **0063** amplia a coluna p/ Text + cifra os valores existentes (backfill app-level Python). Golden (7) + integração (1: insere via ORM, lê a coluna CRUA via `text()` = ciphertext `v1:…` ≠ telefone, lê via ORM = decifrado).
- **Dep:** `cryptography` PROMOVIDA a dep CORE (estava só no grupo opt-in esocial; a CI roda `poetry install --with dev` → o import quebraria tudo. Vinha transitiva via `pyjwt[crypto]`, frágil p/ segurança). `poetry lock` mudou 1 linha (só content-hash, sem churn). **⚠️ pyproject tocado → atenção ao rebase da faxina de ruff (que também edita pyproject, mas em `[tool.ruff]`, seção diferente).**
- **Infra:** os containers `fiscal_*` caíram de novo a meio caminho (parados externamente); subi postgres+redis e segui. DB compartilhado :5434 em **0063**.
- **Validação:** pytest **2725 passed, 3 skipped (+9)** · integração **30 passed (+1)** · mypy 0 (374 arq) · ruff limpo nos meus arquivos. 2 prod-guard tests atualizados (exigem PII key válida) + 2 novos.
- **Estado:** `feat/m3-lgpd-seguranca` = `0a076f2` + 6.1 + 6.2a + 6.2b + 6.3 (a commitar). **NÃO pushado** (freio). Próximo: **6.4 refresh token / rotação JWT** (`POST /v1/auth/refresh` em `app/shared/auth/jwt.py`), depois 6.5 CI gates soft→bloqueante.

### 2026-06-22 · orquestrador · M3 — PR 6.4 (refresh token DB-backed, produção)
O PO confirmou **DB-backed com rotação** e pediu **nível de produção** ("vamos mandar para produção"). Implementado production-grade.
- **PR 6.4 (✅, validado):** `POST /auth/refresh` (montado na `/auth`, não `/v1/auth` — fica junto de login/register no mesmo router; harmonizei o path do briefing à convenção do módulo). Token **opaco** aleatório (384 bits) guardado só como **SHA-256 hex** (`app/shared/auth/refresh_token.py`). Tabela `refresh_token` (migration **0064**, RLS+GRANT; `family_id` encadeia rotação, `revoked_at`). **Rotação** a cada uso + **detecção de reuso** (token revogado reapresentado → revoga a família = anti-roubo). Login/register passam a emitir refresh (resposta += `refresh_token`); a renovação usa `get_system_session` (NOVO dep em `deps.py`: superuser/bypass-RLS, pré-auth, busca por hash global — a credencial é o hash unguessable, o tenant vem do registro). `JWT_REFRESH_EXPIRE_DAYS=30`.
- **Completude (lacuna que eu fechei):** a exclusão LGPD (6.2b) **não** revogava refresh tokens — um titular "esquecido" renovaria acesso por até 30 dias. Liguei `RefreshTokenRepo.revogar_do_tenant` no `lgpd.service.excluir` (resumo += `refresh_tokens_revogados`). Coberto por teste de integração.
- **Decisões de arquitetura:** (1) refresh_token COM RLS (princípio) + renovação por sessão de sistema que bypassa RLS (pré-auth por hash global) — RLS protege contra sessão autenticada de outro tenant; a renovação é fluxo de sistema. (2) `por_hash` UNIQUE global; reuso/rotação na mesma transação, commit antes de levantar no caso de reuso (persiste a revogação da família). (3) Concorrência: 2 refresh simultâneos com o mesmo token → 1 vence, o outro vê revogado → revoga a família (falso-positivo raro; tradeoff conhecido de rotação estrita; cliente refaz login).
- **Nota mypy:** `Result.rowcount` não é tipado (só `CursorResult`) → `revogar_do_tenant` faz select-then-update (mypy-clean, retorna a contagem) em vez de `.rowcount`.
- **Validação:** pytest **2728 passed, 3 skipped (+3)** · integração **35 passed (+5)** · mypy 0 (375 arq) · ruff limpo nos meus arquivos. Suite de auth existente intacta (asserções por `in data`, não shape exato).
- **Estado:** `feat/m3-lgpd-seguranca` = `0a076f2` + 6.1 + 6.2a + 6.2b + 6.3 + 6.4 (a commitar). DB :5434 em **0064**. **NÃO pushado** (freio). Falta só **6.5** (CI gates soft→bloqueante) p/ fechar o M3. Depois M4 (S3, Reinf→SERPRO, e-mail, Dockerfile front).

### 2026-06-22 · orquestrador · M3 — PR 6.5 (gate de segredos) + MARCO 3 COMPLETO
- **PR 6.5 (✅, verificado):** novo job **bloqueante** `secret-scan` no `ci.yml` (em TODO push/PR, repo inteiro — o `qa-gates.yml` é soft e só dispara em mudanças de frontend, então o backend nunca era escaneado). Usa a imagem oficial `zricethezav/gitleaks:v8.30.1` (pinada) + o `.gitleaks.toml`, `--no-git` (working tree do checkout = só arquivos rastreados). **Verifiquei DE VERDADE via Docker** (a frota não tem como rodar isto): scan bruto deu 26 "leaks" → todos em `.venv`/`node_modules`/`.next` (não-versionados, fora do que a CI vê); após excluir esses dirs no `.gitleaks.toml`, **"no leaks found"** sobre a fonte rastreada (inclusive os placeholders dev novos do `config.py` — gitleaks não os flagou). Gates a11y/visual/Semgrep ficam **soft** (baseline de snapshots frontend ausente; Semgrep precisa de triagem de baseline — NÃO promovi o visual sem baseline, conforme o alerta do briefing).
  - **Lição:** `gitleaks --no-git` local varre dirs não-versionados (.venv/node_modules) e infla o resultado; a CI (checkout limpo) só vê arquivos rastreados. Excluir os build dirs no `.gitleaks.toml` torna o scan local representativo. Pinar a imagem do scanner (v8.30.1) > usar a `gitleaks-action` cuja major (@v3) eu não conseguia confirmar.
- **🔐 MARCO 3 COMPLETO (6.1→6.5).** Resumo: security headers; export LGPD (portabilidade + audit) com fix do grant latente do `guia_pagamento`; exclusão LGPD por anonimização (respeita imutabilidade fiscal §8.2 + retenção 5a) + revogação de refresh tokens; AES-256-GCM em repouso (`empresa.whatsapp_phone`, `cryptography` core); refresh token DB-backed com rotação + detecção de reuso; gate de segredos bloqueante. Migrations 0062/0063/0064. **2728 unit, 35 integração, mypy 0 (375 arq), ruff limpo nos arquivos do M3.**
- **Estado final:** worktree isolada `C:/dev/m3-pr61-wt` na `feat/m3-lgpd-seguranca` = `0a076f2` + 6 commits (6.1, 6.2a, 6.2b, 6.3, 6.4, 6.5). DB :5434 em **0064**. **NÃO pushado** (freio).
- **Próximo orquestrador → FECHAMENTO (ações do PO):** (1) tratar a **contaminação** do PR 6.1 no tree principal/chore (comandos no registro de 2026-06-22 PR 6.2a); (2) **consolidar** a `feat/m3-lgpd-seguranca` na `main` por fast-forward (a worktree isolada parte de `0a076f2` = base da `main`, então o ff é limpo); (3) **`git push origin main`** (ato do PO); (4) iniciar o **M4** (S3 storage efetivo, EFD-Reinf→SERPRO, e-mail transacional, Dockerfile do front) — prompt a ser escrito no formato do `docs/PROMPT-PROXIMO-AGENTE-M3.md`.

### 2026-06-22 · orquestrador · Consolidação na main + merge da faxina de ruff
Fechamento do M3 com o PO autorizando (prossiga). Tudo feito na worktree isolada `C:/dev/m3-pr61-wt`; **nada pushado** (freio).
- **Contaminação: auto-resolvida.** A faxina (`chore/ruff-lint-cleanup`) commitou `584a1c6` e **excluiu meus arquivos do PR 6.1** de propósito (a própria msg de commit diz "main.py, security_headers.py + teste FICARAM DE FORA"). Nada a limpar.
- **`main` consolidada por fast-forward** (`git branch -f main feat/m3-lgpd-seguranca`, main não estava em checkout nenhum) → `main = e963229` (M3 completo).
- **Faxina de ruff integrada na `main`** (merge `715e778`): o merge foi quase trivial (1 conflito só — o `_PII_PROD_KEY` do PR 6.3, resolvido mantendo o M3). `ruff check .` agora **VERDE**. **⚠️ Lição (cara):** o `ruff --fix` da faxina QUEBROU mypy em 2 arquivos apesar de ela declarar "pytest/mypy verdes" — confirma a regra "subagente erra; valide à mão". Achados pelo `mypy app/` no tree mergeado e corrigidos: (a) `advisor/service.py` — o autofix do **B009** virou `getattr(digest,"x")`→`digest.x` num param tipado `object` (intencional p/ evitar circular) → tipei como `DigestEstruturado` (o módulo já era importado, sem circular); (b) `tabelas_admin/repo.py` — `dict(rows)` sobre `Sequence[Row]` → `.tuples().all()`.
- **Estado final VERDE e VERIFICADO:** `main` @ `715e778` = M3 (6 PRs) + faxina ruff. **ruff `check .` ✅ · mypy 0 (375 arq) · pytest 2728 passed, 3 skipped · integração 35 passed.** Migrations 0062/0063/0064 no DB :5434. **`origin/main` ainda em `0a076f2` — `main` local está 8 commits à frente, NÃO pushada (ato do PO).**
- **Próximo orquestrador → faça:** confirmar com o PO se a `main` já foi pushada; depois **M4** — prompt completo em `docs/PROMPT-PROXIMO-AGENTE-M4.md`. A branch `chore/ruff-lint-cleanup` já está contida na `main` (pode ser apagada). A worktree `C:/dev/m3-pr61-wt` pode ser removida (a branch persiste no `.git`).

### 2026-06-23 · orquestrador · M4 — PR 1 (Storage S3 efetivo para SPED · item 10)
Sessão nova confirmou que o agente do M3 fechou (push feito): `main` = `origin/main` = `68f4d00`. Baseline VERDE conferido (2731 unit / 3 skip, mypy 0/375, integração 36, head 0064). Início do M4. **Trabalho SOLO** (lição da máquina). Branch `feat/m4-storage-s3`.
- **O que fiz (PR 1):** liguei o object storage (que estava montado no lifespan desde a Sprint 19.6 mas **desconectado de qualquer handler**) ao SPED. O `.txt` (ECD/ECF/EFD-Contrib/EFD-ICMS-IPI) sai do BYTEA (`arquivo_sped.conteudo_bytea`, blobs de 5-50MB no Postgres) e vai pro `app.state.storage` (local/memory/s3). Atrás de env: `STORAGE_BACKEND=s3` + `STORAGE_BUCKET` ligam o S3 sa-east-1 sozinho.
- **Arquivos novos:** `app/shared/storage/deps.py` (`StorageDep`), `app/modules/sped/storage.py` (`chave_storage_sped` / `mover_blob_sped_para_storage` idempotente / `mover_blob_sped_best_effort` / `ler_conteudo_sped`), migration `0065_sped_conteudo_bytea_nullable.py`, `tests/unit/sped/test_storage_sped.py` (10), `tests/integration/test_sped_storage.py` (1). **Tocados:** `app/shared/storage/__init__.py` (`build_storage_from_settings`), `app/shared/db/models.py` (`conteudo_bytea` nullable), 3 routers SPED (`ecd/ecf/efd` — gerar move + download lê), `app/modules/sped/router.py` (download genérico + `validar_sped`), `validacao_service.py` (lê via helper), 2 workers (`sped_gerar_anual`/`sped_gerar_efd_mensal`), 2 testes (`test_router_download_generico` / `test_validacao_service`).
- **Decisão de arquitetura (a melhor lição deste PR):** o move roda na BORDA (router/worker), **o service não muda** — assim os ~37 testes de service ficam intactos (zero churn) E não há caminho dual test/prod divergente (o service sempre persistiu BYTEA; quem move é a borda). A linha é commitada com BYTEA *antes* do move; o move é **best-effort** (S3 fora do ar não derruba uma geração já feita — loga + rollback + segue, BYTEA fica pro fallback). A leitura é **storage-first com fallback BYTEA** → linhas legadas continuam baixando. O `mover_*` é idempotente → a MESMA função serve de backfill das linhas antigas.
- **Validado à mão + integração REAL** (não só pytest): `test_sped_storage.py` insere a linha com BYTEA, move, e confere a **coluna crua** via `text()` (`conteudo_bytea` IS NULL, `storage_key` preenchida), lê de volta do disco e checa idempotência. Migration 0065 aplicada no DB :5434 e conferida (`information_schema` → `is_nullable=YES`). UPDATE de `arquivo_sped` sob RLS já era provado pelo `validacao_service` (sem grant novo: tabela é da Sprint 16).
- **Infra:** Docker `fiscal_postgres`/`fiscal_redis` de pé (healthy, 26h). `alembic.exe` bloqueado pelo Device Guard → usei `python -m alembic` (registre: o `.exe` do `.venv` é bloqueado, igual ao `poetry.exe`).
- **Validação:** pytest **2741 passed, 3 skipped (+10)** · integração **36 passed (+1)** · mypy 0 (377 arq) · ruff limpo nos arquivos tocados.
- **Estado:** `feat/m4-storage-s3` = `68f4d00` + PR 1 (a commitar). DB :5434 em **0065**. **NÃO pushado** (freio). Vou consolidar na `main` por ff e PERGUNTAR ao PO antes do push.
- **Próximo M4:** PR 2 = **EFD-Reinf→SERPRO** (item 11; espelhar `transmissao_esocial_service`, flag `REINF_TRANSMISSAO_ATIVA`), depois e-mail transacional (14) e Dockerfile do front (15). Os outros módulos com blob (notas/DANFSE, pessoal/holerite) podem receber o mesmo tratamento do PR 1 num follow-up (o wiring `StorageDep` + padrão já existem).
